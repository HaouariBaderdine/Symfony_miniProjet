{% extends 'base.html.twig' %}

{% block body %}

    <style>
        main{margin-top: 58px;}
        h2{color:cadetblue;text-align: center;font-weight: bold}
    </style>

    <main role="main">
        <div class="container mt-4">
            <h2>edit Etape </h2>
            {{ form_start(form) }}
            <div class="row">
                <div class="col-md-12">{{ form_row(form.ville_etape) }}</div>
            </div>
            <div class="row">
                <div class="col-md-12">{{ form_row(form.circuit_etape) }}</div>
            </div>
            <div class="row">
                <div class="col-md-12">{{ form_row(form.duree_etape) }}</div>
            </div>
            <div class="row">
                <div class="col-md-12">{{ form_row(form.ordre_etape) }}</div>
            </div>
            <button class="btn btn-primary">Enregistrer</button>
            {{ form_end(form) }}

        </div>
    </main>

{% endblock %}

{% block title %}

{% endblock %}

